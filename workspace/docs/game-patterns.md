# Common Game Patterns

## Project Structure

When writing a game, follow this file layout:

```
workspace/
├── scripts/
│   ├── utils.js          # Pre-loaded utilities (do NOT edit or redeclare)
│   └── game.js           # YOUR game code — write everything here
├── assets/               # Images, audio files (embedded as base64 at build time)
│   ├── player.png
│   ├── enemy.png
│   └── shoot.wav
├── output/
│   └── index.html        # Built game (generated by build_game tool)
└── agent.md              # System constraints (auto-generated, do not modify)
```

**Key rules:**
- Write all game logic in `scripts/game.js` only
- Import from `utils.js` via module scope — classes and functions are already loaded
- Place image and audio files in `assets/` — they're embedded as `window.__ASSETS__[filename]` at build time
- Do not modify or delete existing exports in `utils.js` (you may APPEND new ones). Never modify `agent.md` or files outside `scripts/` and `assets/`.

**Loading assets at runtime:**
```js
const sprites = new SpriteManager();
if (window.__ASSETS__) {
    await sprites.load('player', window.__ASSETS__['player.png']);
}
```

---

## Entity-Component Pattern

Decouple data from behavior. Entities are just IDs, components are pure data, systems contain the logic.

```js
// Component: pure data
class PositionComponent {
    constructor(x, y) { this.x = x; this.y = y; }
}
class VelocityComponent {
    constructor(vx, vy) { this.vx = vx; this.vy = vy; }
}
class RenderComponent {
    constructor(color, w, h) { this.color = color; this.w = w; this.h = h; }
}

// ECS Manager
class ECS {
    constructor() {
        this.entities = new Map();
        this.nextId = 0;
    }

    createEntity() {
        const id = this.nextId++;
        this.entities.set(id, new Map());
        return id;
    }

    addComponent(entityId, component) {
        this.entities.get(entityId).set(component.constructor, component);
    }

    getComponent(entityId, ComponentClass) {
        return this.entities.get(entityId)?.get(ComponentClass);
    }

    query(ComponentClasses) {
        const results = [];
        for (const [id, components] of this.entities) {
            if (ComponentClasses.every(C => components.has(C))) {
                results.push(id);
            }
        }
        return results;
    }
}

// System: operates on entities with specific components
class MovementSystem {
    update(dt, ecs) {
        for (const id of ecs.query([PositionComponent, VelocityComponent])) {
            const pos = ecs.getComponent(id, PositionComponent);
            const vel = ecs.getComponent(id, VelocityComponent);
            pos.x += vel.vx * dt;
            pos.y += vel.vy * dt;
        }
    }
}

// Usage
const ecs = new ECS();
const player = ecs.createEntity();
ecs.addComponent(player, new PositionComponent(100, 100));
ecs.addComponent(player, new VelocityComponent(200, 0));
ecs.addComponent(player, new RenderComponent('blue', 32, 32));
```

**When to use:** Games with many entity types sharing behaviors (bullets, enemies, power-ups). Avoid for simple games with fixed entities.

---

## Object Pooling

Reuse objects instead of allocating new ones, preventing GC pauses.

```js
class ObjectPool {
    constructor(createFn, resetFn, initialSize = 10) {
        this.createFn = createFn;
        this.resetFn = resetFn;
        this.pool = [];
        this.active = new Set();

        // Pre-allocate
        for (let i = 0; i < initialSize; i++) {
            this.pool.push(createFn());
        }
    }

    acquire() {
        let obj = this.pool.pop();
        if (!obj) {
            obj = this.createFn(); // Pool exhausted, create new
        }
        this.active.add(obj);
        return obj;
    }

    release(obj) {
        this.resetFn(obj);
        this.active.delete(obj);
        this.pool.push(obj);
    }

    releaseAll() {
        for (const obj of this.active) {
            this.resetFn(obj);
            this.pool.push(obj);
        }
        this.active.clear();
    }

    getActiveCount() {
        return this.active.size;
    }
}

// Bullet pool example
const bulletPool = new ObjectPool(
    // create
    () => ({ x: 0, y: 0, vx: 0, vy: 0, alive: false, color: '#ff0' }),
    // reset
    (b) => { b.alive = false; }
);

// Fire bullet
const b = bulletPool.acquire();
b.x = player.x; b.y = player.y;
b.vx = Math.cos(angle) * 400;
b.vy = Math.sin(angle) * 400;
b.alive = true;

// Update active bullets
for (const b of bulletPool.active) {
    if (!b.alive) { bulletPool.release(b); continue; }
    b.x += b.vx * dt;
    b.y += b.vy * dt;
}
```

**When to use:** Any game with frequent creation/destruction of objects (bullets, particles, enemies).

---

## Screen Wrapping

Objects that leave one side reappear on the opposite side (Asteroids-style).

```js
function wrapPosition(entity, canvasWidth, canvasHeight, margin = 0) {
    if (entity.x > canvasWidth + margin) entity.x = -margin;
    else if (entity.x < -margin) entity.x = canvasWidth + margin;

    if (entity.y > canvasHeight + margin) entity.y = -margin;
    else if (entity.y < -margin) entity.y = canvasHeight + margin;
}
```

---

## Tile-Based Maps

Efficient grid-based levels using a 2D array.

```js
class TileMap {
    constructor(tileSize) {
        this.tileSize = tileSize;
        this.width = 0;
        this.height = 0;
        this.tiles = [];
    }

    // 0 = empty, 1 = wall, 2 = player spawn, 3 = goal
    loadFromArray(grid) {
        this.tiles = grid;
        this.height = grid.length;
        this.width = grid[0].length;
    }

    getTile(col, row) {
        if (row < 0 || row >= this.height || col < 0 || col >= this.width) {
            return 1; // Out of bounds = wall
        }
        return this.tiles[row][col];
    }

    // World position to tile coordinates
    worldToTile(worldX, worldY) {
        return {
            col: Math.floor(worldX / this.tileSize),
            row: Math.floor(worldY / this.tileSize)
        };
    }

    // Tile coordinates to world position (top-left)
    tileToWorld(col, row) {
        return {
            x: col * this.tileSize,
            y: row * this.tileSize
        };
    }

    isSolid(col, row) {
        return this.getTile(col, row) === 1;
    }

    draw(ctx, cameraX = 0, cameraY = 0) {
        const startCol = Math.floor(cameraX / this.tileSize);
        const startRow = Math.floor(cameraY / this.tileSize);
        const endCol = startCol + Math.ceil(ctx.canvas.width / this.tileSize) + 1;
        const endRow = startRow + Math.ceil(ctx.canvas.height / this.tileSize) + 1;

        for (let row = startRow; row < endRow; row++) {
            for (let col = startCol; col < endCol; col++) {
                const tile = this.getTile(col, row);
                if (tile === 0) continue;

                const { x, y } = this.tileToWorld(col, row);
                ctx.fillStyle = tile === 1 ? '#666' : '#4a4';
                ctx.fillRect(x - cameraX, y - cameraY, this.tileSize, this.tileSize);
            }
        }
    }
}
```

**Tile colors legend:**
- 0 = empty (transparent)
- 1 = wall (solid)
- 2 = player spawn
- 3 = goal/exit
- 4+ = interactive objects (keys, doors, enemies)

---

## Procedural Generation Basics

### Random walk cave generation

```js
function generateCave(width, height, fillProb = 0.45) {
    // Initialize with random noise
    let grid = [];
    for (let y = 0; y < height; y++) {
        grid[y] = [];
        for (let x = 0; x < width; x++) {
            grid[y][x] = Math.random() < fillProb ? 1 : 0;
        }
    }

    // Cellular automata smoothing (3-5 iterations)
    for (let iter = 0; iter < 4; iter++) {
        const newGrid = [];
        for (let y = 0; y < height; y++) {
            newGrid[y] = [];
            for (let x = 0; x < width; x++) {
                let walls = 0;
                for (let dy = -1; dy <= 1; dy++) {
                    for (let dx = -1; dx <= 1; dx++) {
                        const nx = x + dx, ny = y + dy;
                        if (nx < 0 || nx >= width || ny < 0 || ny >= height) {
                            walls++;
                        } else {
                            walls += grid[ny][nx];
                        }
                    }
                }
                // Wall if >= 5 neighbors are walls (including self)
                newGrid[y][x] = walls >= 5 ? 1 : 0;
            }
        }
        grid = newGrid;
    }
    return grid;
}
```

### Simple BSP dungeon rooms

```js
function generateRooms(width, height, maxRooms = 10) {
    const rooms = [];
    for (let i = 0; i < maxRooms * 10 && rooms.length < maxRooms; i++) {
        const rw = 4 + Math.floor(Math.random() * 6);
        const rh = 4 + Math.floor(Math.random() * 6);
        const rx = 1 + Math.floor(Math.random() * (width - rw - 2));
        const ry = 1 + Math.floor(Math.random() * (height - rh - 2));
        const room = { x: rx, y: ry, w: rw, h: rh };

        // Check overlap with existing rooms (with 1-tile padding)
        const overlaps = rooms.some(existing =>
            room.x < existing.x + existing.w + 1 &&
            room.x + room.w + 1 > existing.x &&
            room.y < existing.y + existing.h + 1 &&
            room.y + room.h + 1 > existing.y
        );

        if (!overlaps) rooms.push(room);
    }

    // Connect rooms with L-shaped corridors
    for (let i = 1; i < rooms.length; i++) {
        const a = rooms[i - 1];
        const b = rooms[i];
        const ax = Math.floor(a.x + a.w / 2);
        const ay = Math.floor(a.y + a.h / 2);
        const bx = Math.floor(b.x + b.w / 2);
        const by = Math.floor(b.y + b.h / 2);

        // Horizontal then vertical (L-shape)
        corridorTiles.push(...createCorridor(ax, ay, bx, ay));
        corridorTiles.push(...createCorridor(bx, ay, bx, by));
    }

    return { rooms, corridorTiles };
}
```

---

## Particle Effects

```js
class Particle {
    constructor(x, y) {
        this.x = x;
        this.y = y;
        this.vx = (Math.random() - 0.5) * 200;
        this.vy = (Math.random() - 0.5) * 200 - 100;
        this.life = 1.0;
        this.decay = 1 + Math.random() * 2; // seconds to live
        this.size = 2 + Math.random() * 4;
        this.color = `hsl(${Math.random() * 60 + 20}, 100%, 50%)`;
    }

    update(dt) {
        this.life -= dt / this.decay;
        this.vy += 300 * dt; // gravity
        this.x += this.vx * dt;
        this.y += this.vy * dt;
        return this.life > 0;
    }

    draw(ctx) {
        ctx.globalAlpha = Math.max(0, this.life);
        ctx.fillStyle = this.color;
        ctx.fillRect(this.x - this.size / 2, this.y - this.size / 2, this.size, this.size);
        ctx.globalAlpha = 1;
    }
}

class ParticleSystem {
    constructor() {
        this.particles = [];
    }

    emit(x, y, count = 20) {
        for (let i = 0; i < count; i++) {
            this.particles.push(new Particle(x, y));
        }
    }

    update(dt) {
        for (let i = this.particles.length - 1; i >= 0; i--) {
            if (!this.particles[i].update(dt)) {
                this.particles.splice(i, 1);
            }
        }
    }

    draw(ctx) {
        for (const p of this.particles) {
            p.draw(ctx);
        }
    }
}
```

---

## Easing Functions

Easing functions map normalized time [0,1] to a eased value [0,1].

```js
// Linear
function linear(t) { return t; }

// Quadratic
function easeInQuad(t) { return t * t; }
function easeOutQuad(t) { return t * (2 - t); }
function easeInOutQuad(t) {
    return t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;
}

// Cubic
function easeInCubic(t) { return t * t * t; }
function easeOutCubic(t) { return (--t) * t * t + 1; }
function easeInOutCubic(t) {
    return t < 0.5 ? 4 * t * t * t : (t - 1) * (2 * t - 2) * (2 * t - 2) + 1;
}

// Exponential
function easeOutElastic(t) {
    if (t === 0 || t === 1) return t;
    return Math.pow(2, -10 * t) * Math.sin((t - 0.075) * (2 * Math.PI) / 0.3) + 1;
}

// Bounce
function easeOutBounce(t) {
    if (t < 1 / 2.75) return 7.5625 * t * t;
    if (t < 2 / 2.75) return 7.5625 * (t -= 1.5 / 2.75) * t + 0.75;
    if (t < 2.5 / 2.75) return 7.5625 * (t -= 2.25 / 2.75) * t + 0.9375;
    return 7.5625 * (t -= 2.625 / 2.75) * t + 0.984375;
}

// Usage: animate a value from A to B over 1 second
let t = 0;
const duration = 1; // seconds
let animating = false;

function startAnimation() {
    t = 0;
    animating = true;
}

function updateAnimation(dt) {
    if (!animating) return;
    t += dt / duration;
    if (t >= 1) { t = 1; animating = false; }

    const eased = easeOutBounce(t);
    currentValue = startValue + (endValue - startValue) * eased;
}
```

**Use cases by easing type:**
- `easeOutQuad`: UI elements sliding in, popups
- `easeOutElastic`: Bouncy UI, juicy feedback
- `easeInOutCubic`: Camera movement, smooth transitions
- `easeOutBounce`: "Dropping" effects
- `easeInQuad`: Fade-in effects (simulate light penetration)

---

## State Machine

Game state management with explicit transitions and lifecycle hooks. Prevents mid-frame inconsistencies and makes game flow predictable.

### State Graph

```
                    ┌──────────┐
                    │          │
            ┌──────▶│  MENU    │◀──────┐
            │       │          │       │
            │       └────┬─────┘       │
            │            │             │
            │            ▼             │
            │       ┌──────────┐       │
            │       │          │       │
            │       │ PLAYING  │       │
            │       │          │       │
            │       └────┬─────┘       │
            │            │             │
            │     ┌──────┼──────┐      │
            │     │      │      │      │
            │     ▼      ▼      ▼      │
            │ ┌────┐ ┌──────┐ ┌──────┐ │
            │ │    │ │      │ │      │ │
            └─┤ WIN├─┤PAUSED│ │GAME  │ │
              │    │ │      │ │ OVER │ │
              └────┘ └──────┘ └───┬──┘ │
                                  │    │
                                  └────┘
```

### Implementation

```js
// State enum
const States = {
    MENU: 'menu',
    PLAYING: 'playing',
    PAUSED: 'paused',
    GAME_OVER: 'gameover',
    WIN: 'win'
};

// Allowed transitions: { fromState: [toStates] }
const ALLOWED_TRANSITIONS = {
    [States.MENU]: [States.PLAYING],
    [States.PLAYING]: [States.PAUSED, States.GAME_OVER, States.WIN],
    [States.PAUSED]: [States.PLAYING, States.MENU],
    [States.GAME_OVER]: [States.MENU, States.PLAYING],
    [States.WIN]: [States.MENU, States.PLAYING]
};

let currentState = States.MENU;
let nextState = null;

function changeState(newState) {
    const allowed = ALLOWED_TRANSITIONS[currentState];
    if (!allowed || !allowed.includes(newState)) {
        console.warn(`Invalid transition: ${currentState} -> ${newState}`);
        return;
    }
    // Defer transition to start of next frame
    nextState = newState;
}

function applyStateTransition() {
    if (!nextState) return;
    const prev = currentState;
    onExit(prev);
    currentState = nextState;
    nextState = null;
    onEnter(currentState);
}

function onEnter(state) {
    switch (state) {
        case States.MENU: showMenu(); break;
        case States.PLAYING: resetGame(); break;
        case States.PAUSED: /* nothing special */ break;
        case States.GAME_OVER: saveHighScore(); break;
        case States.WIN: playWinAnimation(); break;
    }
}

function onExit(state) {
    switch (state) {
        case States.PAUSED: /* e.g., hide pause overlay */ break;
        // Clean up any state-specific resources
    }
}

// In game loop:
// function gameLoop(timestamp) {
//     const dt = ...;
//     applyStateTransition(); // Handle deferred transition at start of frame
//     if (currentState === States.PLAYING) update(dt);
//     render();
//     requestAnimationFrame(gameLoop);
// }
```

### When to Use

- Any game with multiple screens (menu, gameplay, results)
- Games with pause functionality
- Games with win/lose conditions that trigger different UI

---

## Responsive Design

Design for a fixed internal resolution and scale to fit any screen while preserving aspect ratio.

### Aspect Ratio Strategies

**Fit (letterbox):** Scale to fit, add black bars if aspect ratio differs.
```js
function fitToContainer(canvas, designW, designH) {
    const parent = canvas.parentElement;
    const maxW = parent.clientWidth || window.innerWidth;
    const maxH = parent.clientHeight || window.innerHeight;
    const scale = Math.min(maxW / designW, maxH / designH);

    canvas.width = designW;
    canvas.height = designH;
    canvas.style.width = `${designW * scale}px`;
    canvas.style.height = `${designH * scale}px`;
    // Centers automatically if parent uses flexbox
}
```

**Fill (crop):** Scale to fill, crop excess. Good for full-screen games.
```js
function fillContainer(canvas, designW, designH) {
    const parent = canvas.parentElement;
    const maxW = parent.clientWidth || window.innerWidth;
    const maxH = parent.clientHeight || window.innerHeight;
    const scale = Math.max(maxW / designW, maxH / designH);

    canvas.width = designW;
    canvas.height = designH;
    canvas.style.width = `${designW * scale}px`;
    canvas.style.height = `${designH * scale}px`;
}
```

### Mouse/Touch Coordinate Mapping

When CSS size differs from canvas resolution, scale input coordinates:

```js
function canvasCoords(e) {
    const rect = canvas.getBoundingClientRect();
    return {
        x: (e.clientX - rect.left) * (canvas.width / rect.width),
        y: (e.clientY - rect.top) * (canvas.height / rect.height)
    };
}
```

### Container CSS for Centering

```css
body {
    margin: 0;
    overflow: hidden;
    background: #000;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}
canvas {
    display: block;
}
```

For pixel art, add:
```css
canvas {
    image-rendering: pixelated;
    image-rendering: crisp-edges;
}
```

### Recommended: Use setupCanvas

The `setupCanvas` utility from utils.js uses the "fit" strategy with pixel-art rendering:

```js
const canvas = setupCanvas('gameCanvas', 800, 600);
const ctx = canvas.getContext('2d');
// Handles resize, pixel rendering, and centering
```

---

## Input Buffering

Queue player inputs to process them at a fixed rate, preventing lost inputs between frames and enabling frame-rate-independent game logic.

### Direction Queue (for grid-based games like Snake)

```js
class InputBuffer {
    constructor(maxLength = 3) {
        this.queue = [];
        this.maxLength = maxLength;
        this.opposites = {
            'up': 'down', 'down': 'up',
            'left': 'right', 'right': 'left'
        };
    }

    enqueue(direction) {
        const last = this.queue[this.queue.length - 1];
        // Ignore if same as last queued direction
        if (direction === last) return;
        // Ignore opposite of last queued direction
        if (direction === this.opposites[last]) return;
        if (this.queue.length < this.maxLength) {
            this.queue.push(direction);
        }
    }

    dequeue() {
        return this.queue.shift() || null;
    }

    clear() {
        this.queue = [];
    }
}

// Usage:
const inputBuffer = new InputBuffer();

// In input handler:
inputBuffer.enqueue('up');

// In fixed-step update (e.g., snake movement tick):
const dir = inputBuffer.dequeue();
if (dir) snake.direction = dir;
```

### Action Buffer (for action/fighting games)

Buffer attack inputs and perform on the earliest valid frame:

```js
class ActionBuffer {
    constructor(windowTime = 0.15) {
        this.actions = [];
        this.windowTime = windowTime;
    }

    queue(action) {
        this.actions.push({ action, time: 0 });
    }

    update(dt, canExecute) {
        for (let i = this.actions.length - 1; i >= 0; i--) {
            this.actions[i].time += dt;
            // Remove expired inputs
            if (this.actions[i].time >= this.windowTime) {
                this.actions.splice(i, 1);
                continue;
            }
            // Execute if valid
            if (canExecute(this.actions[i].action)) {
                this.actions.splice(i, 1);
                return true;
            }
        }
        return false;
    }

    clear() {
        this.actions = [];
    }
}

// Usage:
const actions = new ActionBuffer(0.15);
actions.queue('punch');

// In update():
actions.update(dt, (action) => {
    if (action === 'punch' && isGrounded()) {
        doPunch();
        return true;
    }
    return false;
});
```

### When to Use

- **Direction queue:** Grid-based movement (Snake, Pac-Man, puzzle games)
- **Action buffer:** Fighting games, combo systems, rhythm games
- **Generic key buffer:** Any game where inputs might arrive between fixed update ticks
